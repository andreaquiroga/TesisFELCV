<%= form_for(@direct_action) do |f| %>
  <% if @direct_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@direct_action.errors.count, "error") %> prohibited this direct_action from being saved:</h2>

      <ul>
      <% @direct_action.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
